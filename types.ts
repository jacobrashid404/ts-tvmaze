// type to represent a show
type tShow = {
  id: number,
  name: string,
  summary: string,
  image: string
}

// type to represent an episode
type tEpisode = {
  name: string,
  season: number,
  number: number
}